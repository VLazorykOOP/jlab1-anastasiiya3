/**
 * Клас FactorialCalculator містить метод для обчислення факторіалу числа.
 */
public class FactorialCalculator {

    /**
     * Основний метод (main), з якого починається виконання програми.
     */
    public static void main(String[] args) {
        // Число, факторіал якого ми хочемо обчислити
        int number = 5; 
        
        // Виклик методу для обчислення факторіалу
        long result = calculateFactorial(number); 
        
        // Виведення результату на консоль
        System.out.println("Факторіал числа " + number + " дорівнює: " + result); 
    }

    /**
     * Метод для обчислення факторіалу невід'ємного цілого числа (n!).
     * Факторіал n - це добуток усіх натуральних чисел від 1 до n.
     * * @param n невід'ємне ціле число
     * @return факторіал числа n (довгий тип для уникнення переповнення)
     */
    public static long calculateFactorial(int n) {
        // Перевірка на від'ємне число
        if (n < 0) {
            throw new IllegalArgumentException("Факторіал не визначений для від'ємних чисел.");
        }
        
        // Факторіал 0 та 1 дорівнює 1
        if (n == 0 || n == 1) {
            return 1;
        }

        // Ініціалізація змінної для зберігання добутку
        long factorial = 1;
        
        // Цикл для множення чисел від 2 до n
        for (int i = 2; i <= n; i++) {
            factorial = factorial * i;
        }

        return factorial;
    }
}

